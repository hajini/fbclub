import 'package:flutter/material.dart';

class Tab03Screen extends StatelessWidget {
  const Tab03Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('3번 화면'),
    );
  }
}
