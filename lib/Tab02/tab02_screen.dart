import 'package:flutter/material.dart';

class Tab02Screen extends StatelessWidget {
  const Tab02Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('2번 화면'),
    );
  }
}