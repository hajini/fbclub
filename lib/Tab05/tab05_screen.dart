import 'package:flutter/material.dart';

class Tab05Screen extends StatelessWidget {
  const Tab05Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('5번 화면'),
    );
  }
}