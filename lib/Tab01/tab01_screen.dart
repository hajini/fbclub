import 'package:flutter/material.dart';

class Tab01Screen extends StatelessWidget {
  const Tab01Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('1번 화면'),
    );
  }
}