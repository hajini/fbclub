import 'package:flutter/material.dart';

class Tab04Screen extends StatelessWidget {
  const Tab04Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('4번 화면'),
    );
  }
}
